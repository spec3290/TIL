2025-12-15 MVC

MVC는 소프트웨어를 Model, View, Controller 3가지 요소로 분리하는 패턴이다.
데이터, 화면, 사용자 입력 처리를 나눠서 객체지향적으로 처리하기에 
이해를 쉽게 하고 유지보수성을 높인다는 장점이 있다.

전체적인 흐름은

사용자 입력

controller(요청 받음)

model(데이터 처리, 로직수행)

view(표시 데이터 렌더링)

http response로 사용자에게 응답
