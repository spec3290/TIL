트랜잭션 격리 레벨이란?
특정한 트랜잭션이 다른 트랜잭션의 동작을 얼마나 격리하는지에 대한 레벨.

- **READ UNCOMMITTED** (가장 낮음)
    - 커밋되지 않은 다른 트랜잭션의 변경 내용을 읽을 수 있다. 
    
- **READ COMMITTED**
    - 커밋된 데이터만 읽을 수 있다.(postgresql 기본값)

- **REPEATABLE READ**
    - 트랜잭션 시작 전에 커밋된 데이터만 조회하며, 실행 중 다른 트랜잭션의 갱신을 차단합니다. MySQL의 기본값
    
- **SERIALIZABLE** (가장 높음)
    - 가장 엄격한 단계로, 동시성 처리가 느려질 수 있지만 완벽한 데이터 정합성을 보장합니다.

여기서 READ UNCOMMITTED는 커밋되지 않은 변경 내용도 읽을 수 있어서 데이터가 일정하지 않을수 있다는 단점이 있고, SERIALIZABLE은 가장 엄격하게 제한하는 만큼 동시성 처리가 느려진다는 단점이 있다.

트랜잭션 격리 레벨에 따라 발생하는 다양한 현상들이 존재하는데
# Dirty read
트랜잭션에서 조회 시 커밋되지 않은 데이터를 조회하는 현상.
작업 중에 다른 트랜잭션이 작업하고 커밋 직전 데이터를 읽었는데, 만약 그 트랜잭션에서 롤백이 나온다면?. 원본 데이터는 롤백됐는데 읽어온 데이터는 이상한 데이터가 되는 것.

# Non-repeatable read
한 트랜잭션에서 같은 데이터 두번 조회하는데, 그 데이터 값이 처음 조회했을 때랑 다음에 조회했을때 값이 다른것.
중간에 다른 트랜잭션이 껴서 그 값을 update해서 그런것
문제는 처음이랑 다르니까 데이터 일관성이 깨진다

# Phantom read
첫 조회 이후 두번째 조회 때 행의 수가 변하는 것.
non repeatable read랑 비슷하다고 생각할 수 있는데 명확한 차이는 non repeatable read는 데이터 값이 바뀌는 것이고 phantom read는 행이 추가/삭제되는 것이다.

| 수준                   | Dirty Read | Non-repeatable | Phantom* | 핵심/비용                                          |
| -------------------- | ---------- | -------------- | -------- | ---------------------------------------------- |
| **READ_UNCOMMITTED** | 가능         | 가능             | 가능       | 실무 거의 미사용. PostgreSQL은 RC처럼 동작(실질 미지원)         |
| **READ_COMMITTED**   | ❌          | 가능             | 가능       | 문장 단위 스냅샷. 빠르고 실용적. PostgreSQL 기본              |
| **REPEATABLE_READ**  | ❌          | ❌              | 보통 ❌     | 트랜잭션 단위 스냅샷(Snapshot Isolation). Write-Skew 가능 |
| **SERIALIZABLE**     | ❌          | ❌              | ❌        | 직렬 가능성 보장. 충돌 시 커밋 실패→재시도 필요, 비용↑              |