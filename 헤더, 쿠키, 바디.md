브라우저에서 로그인 요청을 보낸다 칠 때

```
POST /auth/login HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer abc.def.ghi
Cookie: refreshToken=xyz123

{
  "email": "test@test.com",
  "password": "1234"
}
```
대충 이렇다고 하면
크게 3부분으로 나뉘는데

http메서드+경로->시작줄이라고 함
### 헤더
요청이나 응답에 대한 메타데이터를 담는 공간
대표적인 헤더 : Authorization. JWT 인증 시 많이 사용함

### 바디
실제 데이터를 담는 공간. 이건 별거 없으니 넘어가고

### 쿠키
서버가 요청 받고 응답할때 쿠키를 만들어서 브라우저한테 붙여줌. 그 이후로 브라우저는 요청 보낼 때마다 그 쿠키를 포함시켜서 보냄.

보통 서버가 리프레시토큰을 넣어둬서 사용함